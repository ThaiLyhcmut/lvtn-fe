import { LoginPage } from "@/components/login-page"

export default function HomePage() {
  // In a real app, check if user is authenticated
  // For demo, always show login page
  return <LoginPage />
}
